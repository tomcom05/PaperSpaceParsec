I have the script setup to run out of C:\Files. If you don't want to put the files somewhere else, just change the directory

You also need to update the scripts with your PaperSpace API Key, Paperspace Server ID, & Parsec Server ID where noted in the scripts.
